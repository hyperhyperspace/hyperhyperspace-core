import { Identity, RSAKeyPair, RSAPublicKey } from 'data/identity';


describe('Identity', () => {
    test( 'Basic identity', () => {
        let id = new Identity();
        let keyPair = new RSAKeyPair();
        let publicKey = new RSAPublicKey;

        id; keyPair; publicKey;


    });
});